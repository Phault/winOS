import styled from 'styled-components/macro';
import { List } from './list/List';

export const ProgramList = styled(List)`
    hr {
        width: 172px;
        background: linear-gradient(to right, transparent, #D0D0C6, transparent);
        height: 1px;
    }
`;